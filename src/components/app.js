import React from 'react';
import Stopwatch from './stopwatch';

export default (props) => {
    return (
        <div className="container">
            <h1 className="text-center"> React Stopwatch</h1>
            <Stopwatch/>
        </div>
    )
}
